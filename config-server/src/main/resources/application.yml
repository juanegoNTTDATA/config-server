server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/juanegoNTTDATA/bootCamp-ConfigServer.git
        default-label: master
        search-paths:
          - '*service*'

